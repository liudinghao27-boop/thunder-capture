# Matrix Stabilization Baseline

**Date:** 2026-06-20  
**Branch:** `feat/phase1-export-compliance-new`  
**Commit:** `b4ecd5b`  
**Python:** `3.14.3`

## Test Baseline

Command:

```powershell
$tempRoot=Join-Path (Resolve-Path .) 'tmp\pytest'
$env:TEMP=$tempRoot
$env:TMP=$tempRoot
.\.venv\Scripts\python.exe -m pytest tests -q --tb=short -p no:cacheprovider --basetemp "$tempRoot\run"
```

Result:

- Exit code: `0`
- Passed: `220`
- Failed: `0`
- Errors: `0`
- Warnings: `2`

Warnings:

1. Starlette `TestClient` reports that its current `httpx` integration is deprecated in favor of `httpx2`.
2. `tests/server/api/test_abtest.py::test_add_variant_success` leaves an `AsyncMock` coroutine unawaited.

The default system temporary directory is not writable in the managed execution environment. Tests must use a project-local ignored temporary directory or an explicitly writable CI temp directory.

## Ruff Baseline

Command:

```powershell
.\.venv\Scripts\python.exe -m ruff check core server adapters tests cli.py
```

Result: exit code `0`, `All checks passed!`

## Mypy Baseline

Command:

```powershell
.\.venv\Scripts\python.exe -m mypy core server adapters cli.py --ignore-missing-imports
```

Result: exit code `0`, no issues found in `95` source files.

Mypy emitted annotation notes for untyped function bodies in `core/session.py` and `core/classify.py`; these are not type errors.

## Migration Baseline

Database: `tmp/plan_validation.db`

Commands:

```powershell
$env:THUNDER_DATABASE_URL='sqlite:///tmp/plan_validation.db'
.\.venv\Scripts\alembic.exe upgrade head
.\.venv\Scripts\alembic.exe check
```

Applied revisions:

- `5b4bf33fdcdb`: initial schema
- `5319c25cfe88`: scheduling and effect fields
- `1c965b3067f5`: reply variants
- `81fe0505b506`: task queue composite indexes

Result: exit code `0`, `No new upgrade operations detected.`

## Release Baseline Decision

The automated Python baseline is green. The next blocking work is API contract coverage, strict cancellation state transitions, evidence-based send confirmation, browser E2E coverage and 30-device scheduling simulation.
