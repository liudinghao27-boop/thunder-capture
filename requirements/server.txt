# Thunder Capture Server — additional dependencies
# Install: pip install -r requirements-server.txt
# These are required in addition to the main requirements.txt / pyproject.toml

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
bcrypt>=4.0.0
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0
openai>=1.50.0
httpx>=0.27.0
pydantic>=2.10.0
python-multipart>=0.0.9
json-repair>=0.30.0
easyocr>=1.7.2

# ── Optional: AutoGLM PhoneAgent (for DM sending) ──
# Install from Open-AutoGLM subdirectory:
#   cd Open-AutoGLM && pip install -e .
# This provides the 'phone_agent' package used by engine/sender.py
